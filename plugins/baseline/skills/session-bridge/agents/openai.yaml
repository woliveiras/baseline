interface:
  display_name: "Session Bridge"
  short_description: "Create truthful resumable session handoffs"
  default_prompt: "Use $session-bridge to capture a truthful handoff for the next session."
policy:
  allow_implicit_invocation: true
