# SPEC-0008 behavior and oracle matrix

| Criterion | Scenario | Invariant | Observable oracle | Oracle provenance | Planned verification | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| RV-001 | Bootstrap version | Every current product-version source agrees | Parsed root package, plugin manifest, and release manifest each equal `0.1.0` | spec-derived | unit test | pass; focused and full unit suites |
| RV-002 | Automated version bump | One root package owns tag and plugin version updates | Parsed config has one `.` Node package, pre-1.0 settings, `v` tag settings, and plugin JSONPath extra file | spec-derived | unit test; Release Please manifest inspection | pass; unit test and official 17.6.0 schema |
| RV-003 | Human release policy | Release meaning and authority are durable | Changelog and release guide contain initial version, increment table, release boundary, rollback, and no-npm contract | independent | documentation test; three-phase review | pass; documentation oracle and phase reviews |
| RV-004 | Stable consumer install | Stable users do not follow mutable `main` | README uses `--ref v0.1.0` for stable install and documents full replacement with `v0.2.0`; `main` is labeled development-only | spec-derived | documentation test; CLI help inspection | pass locally; tag-pinned remote install pending publication |
| RV-005 | Pull request and main validation | Required deterministic evidence runs without provider calls or write scope | Workflow/composite action contains every required command, read-only permissions, pinned actions/validator source, and excludes `eval:full`/provider suites | independent | workflow contract test; fresh CI run | pass; local suite and PR #1 GitHub Actions `Validate` |
| RV-006 | Release token isolation | Write credentials never execute repository content; generated PR gets equivalent validation | Release workflow mutation job has no checkout/repo command, validation job is read-only, and no-checkout status job targets the API-resolved head SHA | independent | workflow contract test; security review; generated Release PR evidence | pass static/security review; first generated Release PR remains empirical evidence |
| RV-007 | Protected default branch | Release candidates cannot bypass the validation contract | GitHub API reports PR requirement, required `Validate`, admin enforcement, linear history, conversation resolution, and no force/delete | external | GitHub branch-protection API inspection | pass; GitHub API inspection after PR #1 initial check |
| RV-008 | Initial publication | Tag and GitHub Release are one immutable release identity | Git/GitHub APIs report `v0.1.0` and Release target at the protected merge commit; npm remains unpublished | external | Git/GitHub inspection | pending |
| RV-009 | Tag-pinned Codex installation | Released source installs independently of checkout and authentication | Disposable `HOME`/`CODEX_HOME` installs `tuxedo@tuxedo` from `v0.1.0`, lists 17 skills, and reports version `0.1.0` | external | clean-room Codex CLI lifecycle | pending |
| RV-010 | Next feature release | A user-visible feature advances `0.1.0` to `0.2.0` without publication before merge | Release Please dry-run or generated PR changes every version source/changelog to `0.2.0`; tag/release remain absent before merge | spec-derived | Release Please behavior inspection; first future feature PR | config passes official schema; live feature PR pending after bootstrap |
