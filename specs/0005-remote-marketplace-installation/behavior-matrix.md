# SPEC-0005 behavior and oracle matrix

| Criterion | Scenario | Invariant | Observable oracle | Oracle provenance | Planned verification | Evidence |
| --- | --- | --- | --- | --- | --- | --- |
| RM-001 | Fresh machine without a checkout | Remote installation adds the GitHub marketplace before selecting the plugin | README contains the exact `marketplace add ... --ref main` and `plugin add tuxedo@tuxedo` sequence and explains the checkout is unnecessary | spec-derived | deterministic documentation test; local CLI help inspection | pass; focused test and CLI help |
| RM-002 | Optional sparse fetch | Both the marketplace manifest and package are present in a sparse snapshot | One documented command contains `--sparse .agents/plugins/marketplace.json` and `--sparse plugins/tuxedo` | spec-derived | deterministic documentation test; local CLI help inspection | pass; focused test and CLI help |
| RM-003 | Private repository | GitHub authentication is delegated to the machine's SSH/Git transport | README contains the SSH URL alternative and no credential-bearing URL or secret material | external | deterministic documentation test; command inspection | pass; focused test, CLI help, and remote SSH clean-room lifecycle |
| RM-004 | Update, reinstall, and removal | Lifecycle operations refresh in the correct order and are distinct | README contains `marketplace upgrade`, plugin remove/add, and plugin-before-marketplace removal order | spec-derived | deterministic documentation test; local CLI help inspection | pass; focused test and CLI help |
| RM-005 | Authentication boundary | Codex login and GitHub Git access are not conflated | README explicitly separates the two and states no credentials belong in URLs or the repository | spec-derived | deterministic documentation test; secret-pattern scan | pass; focused test |
| RM-006 | Mutable source and unsupported direct URL | `main` is documented as mutable and no unpublished tag or direct plugin URL route is implied | README contains mutable-main/no-tags limitations and contains no `codex plugin add` invocation whose argument is a URL | spec-derived | deterministic documentation test; repository tag inspection | pass; focused test and empty local tag list |
| RM-007 | Local development | A trusted local clone remains available for repository development | README and development guide preserve `git clone`, local marketplace add, and local package source context | spec-derived | deterministic documentation test | pass; focused test |
| RM-008 | Scope boundary | Documentation tests do not mutate plugin state or call providers | Test source contains no plugin-install execution and focused command runs only static tests | independent | test review and command evidence | pass; focused static test; later installation probes remained separate |
| RM-009 | Current private repository | Fresh installation uses a transport that can authenticate to the current repository without conflating Codex login | README identifies SSH as the verified route and conditions the HTTPS shorthand on public access or separately configured GitHub HTTPS credentials | external | deterministic documentation test; isolated remote clean-room probes | pass; anonymous HTTPS failed as expected for private access, SSH lifecycle passed |
| RM-010 | Marketplace identity | Product identity is independent of local or remote transport | Manifest name is `tuxedo`, display name is `Tuxedo`, maintained selectors are `tuxedo@tuxedo`, and current installation docs contain no location-branded marketplace name | spec-derived | focused manifest and documentation tests; local Codex reinstall | pass; focused tests, isolated lifecycle, and installed state |
