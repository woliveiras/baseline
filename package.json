{
  "name": "baseline",
  "version": "0.4.0",
  "private": true,
  "packageManager": "pnpm@11.13.1",
  "description": "Development-only evaluation tooling for Baseline",
  "engines": {
    "node": ">=22.22.0"
  },
  "scripts": {
    "eval:login": "uv run --locked python evals/promptfoo/scripts/codex_auth.py login",
    "eval:auth:status": "uv run --locked python evals/promptfoo/scripts/codex_auth.py status",
    "eval:smoke": "uv run --locked python evals/promptfoo/scripts/run-evaluations.py --suite smoke",
    "eval:routing": "uv run --locked python evals/promptfoo/scripts/run-evaluations.py --suite routing",
    "eval:skills": "uv run --locked python evals/promptfoo/scripts/run-evaluations.py --suite skills",
    "eval:security": "uv run --locked python evals/promptfoo/scripts/run-evaluations.py --suite security",
    "eval:compare": "uv run --locked python evals/promptfoo/scripts/run-evaluations.py --suite compare",
    "eval:redteam:generate": "uv run --locked python evals/promptfoo/scripts/run-evaluations.py --suite redteam-generate",
    "eval:redteam:review": "uv run --locked python evals/promptfoo/scripts/run-evaluations.py --suite redteam-review",
    "eval:redteam:full": "uv run --locked python evals/promptfoo/scripts/run-evaluations.py --suite redteam-full",
    "eval:full": "uv run --locked python evals/promptfoo/scripts/run-evaluations.py --suite full"
  },
  "devDependencies": {
    "@openai/codex-sdk": "0.146.0",
    "promptfoo": "0.122.0"
  }
}
