[project]
name = "tuxedo"
version = "0.1.0"
description = "Development tooling for Tuxedo"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "PyYAML==6.0.2",
]

[tool.uv]
package = false
