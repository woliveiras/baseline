[project]
name = "baseline"
version = "0.3.0"
description = "Development tooling for the Baseline engineering foundation"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "PyYAML==6.0.2",
]

[tool.uv]
package = false
